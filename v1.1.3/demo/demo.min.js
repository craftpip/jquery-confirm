if(typeof jQuery == 'undefined'){
    throw new Error('Documentation needs the jQuery library to function.');
}

$(document).ready(function () {
    prettyPrint();
    $('span.version').html(version || '');
});
$(".gotoSmooth").click(function () {
    var b = $("html, body"), a = $(this).attr("href");
    return b.animate({scrollTop: $(a).offset().top - 60}, {queue: !1, duration: 500, easing: "easeOutExpo"}), !1
});
$.easing.jswing = $.easing.swing, $.extend($.easing, {easeOutQuart: function (f, d, c, b, g) {
        return -b * ((d = d / g - 1) * d * d * d - 1) + c
    }, easeOutExpo: function (f, d, c, b, g) {
        return d == g ? c + b : b * (-Math.pow(2, -10 * d / g) + 1) + c
    }, easeOutElastic: function (h, f, d, c, k) {
        var b = 1.70158, j = 0, g = c;
        if (0 == f) {
            return d
        }
        if (1 == (f /= k)) {
            return d + c
        }
        if (j || (j = 0.3 * k), g < Math.abs(c)) {
            g = c;
            var b = j / 4
        } else {
            var b = j / (2 * Math.PI) * Math.asin(c / g)
        }
        return g * Math.pow(2, -10 * f) * Math.sin((f * k - b) * 2 * Math.PI / j) + c + d
    }});